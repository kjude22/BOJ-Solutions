
   
**  unordered_set 👉 “key만 저장하는 해시 테이블”
    unordered_map 👉 “(key, value) pair를 저장하는 해시 테이블”
    둘 다 해시 테이블(Hash Table) 이고,
    차이는 “원소 타입” 뿐

**  해시맵은 배열/벡터처럼 인덱스로 접근 ❌
    오해 포인트) um[3] : ❌ “3번째 원소”  ✅ “key = 3 인 value”

## 해시맵 헤더

    #include <unordered_set>
    #include <unordered_map>


    <선언>
    unordered_set<T> us;    해시, 중복 ❌, 평균 O(1)
    unordered_map<K, V> um; 해시, 키 중복 ❌, 평균 O(1)


    <삽입>
    us.insert(10);

    um.insert({1, 100});
    um[2] = 200;            key 없으면 생성 (주의!)
    um.emplace(3, 300);     불필요한 복사 방지 (권장)


    <탐색>
    us.find(10)             iterator 반환 (없으면 us.end())
    um.find(1)              iterator 반환 (없으면 um.end())
    us.count()              유무만 확인 (0/1)

    <삭제>
    us.erase(10);           값 / 키로 삭제
    um.erase(1);

    us.erase(us.find(20));  iterator로 삭제
    um.erase(um.find(2));


    <크기/비어있는지>
    us.size() / us.empty();
    um.size() / um.empty();


