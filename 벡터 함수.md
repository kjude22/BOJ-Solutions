
## vector 헤더

    vector<int> v;          int, char, string 모두 지원

    v.push_back(3);         맨 뒤에 원소 추가      
    v.pop_back();           맨 뒤의 원소 삭제
    v.clear();              벡터 초기화

    v.[0]
    v.front();
    v.back();

    v.size();               원소의 개수 반환
    v.empty();              비어있는지 확인 (bool)

    for (int n : v)         벡터 전체 순회
    {
        cout << n;          벡터 원소들을 순서대로 출력
    }


## algorithm 헤더

    **<선형 탐색 - O(N)>**
    int idx = find(v.begin(), v.end(), 30) - v.begin();         30을 찾고 iterator 반환
    

    **<이진 탐색 - O(log N)>**
    int lb = lower_bound(v.begin(), v.end(), x) - v.begin();    x 이상 첫 위치
    int ub = upper_bound(v.begin(), v.end(), x) - v.begin();    x 초과 첫 위치


    replace(v.begin(), v.end(), 20, 99);            20을 찾고 99로 교체

    sort(v.begin(), v.end());   

    sort(p.begin(), p.end(),                        사용자 정의 정렬
          [](const Person& a, const Person& b) {
              return a.age < b.age;
          });

    v.erase(unique(v.begin(), v.end()), v.end());   중복제거 (*반드시 정렬 후 사용*)

    reverse(v.begin(), v.end());

    