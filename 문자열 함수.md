
## string 헤더
    
    s.length(); s.empty();              길이 및 상태 확인
    s.push_back('!'); s.pop_back();     문자열 추가 및 수정

    string sub = s.substr(3, 2);        문자열 자르기  (인덱스 3부터 두 글자)
    string sub2 = s.substr(3);                        (인덱스 3부터 끝까지)

    str.find("찾을내용")                찾는 문자열이 처음 등장하는 인덱스를 반환
    string s = to_string(num)           숫자를 문자열로 변환
    int n = stoi(s);                    문자열을 int로 변환
    long long ll = stoll(s)             문자열을 long long로 변환

    ==, !=, <, >                        비교 연산자로 사전순 비교

    getline(cin, s);                    엔터(\n) 전까지 문자열을 읽음
    getline(cin, s, ',');               콤마(,) 전까지 문자열 읽음

## algorithm 헤더

    reverse(s.begin(), s.end());        문자열 뒤집기
    sort(s.begin(), s.end());           문자열 정렬
