
**  비어있는 상태에서 pop()하면 SegFault 발생! 
    - 예외처리 필요

## stack 헤더

    stack<int> s;

    s.push(x);
    s.pop();            ※ iterator 없음 - 순회 불가

    s.top();

    s.empty();
    s.size();

## queue 헤더

    queue<int> q;

    q.push(x);
    q.pop();            ※ iterator 없음 - 순회 불가

    q.front();
    q.back();

    q.empty();
    q.size();
